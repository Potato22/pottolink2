---

---

<div class="mtBox">
  <div class="mtObj">?</div>
</div>

<script>
  import "./miniToastLogic"
</script>

<style lang="scss">
  .mtBox {
    position: fixed;
    z-index: 1000;
    width: 100vw;
    height: 100vh;
    display: grid;
    //display: none;
    place-items: center;
    font-family: var(--content);
    pointer-events: none;
    color: var(--fore);
  }
  .mtObj {
    background: rgba(var(--baseRGB), 0.6);
    backdrop-filter: var(--acrylicBackdrop);
    color: var(--text);
    padding: 0.5em 1em;
    border-radius: 20px;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
    max-width: 600px;
    margin: 0 1em;
    text-align: center;
    font-size: 1.2rem;
    
    transform: scale(0.8);
    opacity: 0;
    transition: all 0.3s var(--sleek);

    &.show {
      transform: scale(1);
      opacity: 1;
    }
  }
</style>
